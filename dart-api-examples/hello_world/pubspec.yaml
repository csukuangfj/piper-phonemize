name: piper_phonemize_hello_world
description: Example showing piper-phonemize phonemization via Dart CLI.
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  piper_phonemize: ^1.3.4
  # piper_phonemize:
  #   path: ../../flutter/piper_phonemize
  path: ^1.9.0
