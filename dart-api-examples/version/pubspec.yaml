name: piper_phonemize_version_example
description: Example showing piper_phonemize version info via Dart CLI.
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  piper_phonemize: ^1.3.0
  # piper_phonemize:
  #   path: ../../flutter/piper_phonemize
  path: ^1.9.0
