name: hello_world
description: >
  A minimal Flutter example that displays piper-phonemize version information.

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  piper_phonemize: ^1.4.8
  # piper_phonemize:
  #   path: ../../flutter/piper_phonemize

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
